const config = {
  bucketName: 'homeworkout-esi',
  dirName: 'lesson_photos',
  region: 'us-east-2',
  accessKeyId: process.env.ACCESS_KEY_ID,
  secretAccessKey: process.env.SECRET_ACCESS_KEY,
}

export default config
